<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="collecti.on.collection"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET" />
    

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Black.NoTitleBar" >
        <activity
            android:name="collecti.on.collection.Login"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name="collecti.on.collection.Signup"
            android:screenOrientation="portrait" >
        </activity>
        <activity 
            android:name="collecti.on.collection.BrowseCollections"
            android:screenOrientation="portrait" >
        </activity>
        <activity 
            android:name="collecti.on.collection.UserProfile"
            android:screenOrientation="portrait" >
        </activity>
        <activity 
            android:name="collecti.on.collection.AddCollection"
            android:screenOrientation="portrait" >
        </activity>
    </application>

</manifest>